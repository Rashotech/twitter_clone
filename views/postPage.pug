extends layouts/main-layout

block content
    script. 
        var postId = '!{postId}' 
    .postsContainer
     
    +createReplyModal(userLoggedIn)
     
block scripts 
     script(src="/js/postPage.js") 